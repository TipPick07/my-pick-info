# 팁픽 일일 자동배포 점검 가이드

> 매일 자동배포(KST 05:23 cron) 성공 후, 새 크롤링 데이터·블로그 글이 우리가 정한
> 규칙/형식을 지켰는지, 그리고 전날 작업이 라이브에 반영됐는지 점검하기 위한 문서.
> **크롤링 주제는 매일 달라서 중간에 예상 못 한 형식/요소가 튀어나온다.** 그걸 놓치지 않는 게 목적.

---

## 사용법 (매일 아침 루틴)

1. **자동배포 성공 확인** — GitHub Actions(Deploy to Cloudflare Pages) 또는 배포 로그가 성공인지.
2. **Claude Code 새 세션을 열고**, 다음 한 줄만 지시하면 된다:
   > `docs/daily-deploy-inspection.md 읽고 오늘(2026-06-04) 기준으로 종합 점검 실행해줘.`
   - 날짜만 그날로 바꾼다. (이 파일이 레포에 있으므로 어느 세션이든 읽어서 그대로 실행 가능.)
3. **섹션별 PASS / WARN / FAIL 보고**를 받는다.
4. FAIL·의심 건이 있으면 **수정안을 먼저 확인**하고, 승인 후 수정. **빌드 검증·커밋/푸시는 내가 지시할 때만 1회.**
5. 약 **1주일간 매일** 돌린다. 안정화되면 빈도를 줄인다(주 1~2회 등).

> 핵심 규칙(절대 변하지 않음): ① 수정은 승인 후 ② 커밋/푸시는 지시 시에만 ③ 빌드 검증은 점검·수정 다 끝난 뒤 한 번만.

---

## 점검 프롬프트 (그대로 실행)

```
[팁픽 일일 자동배포 점검 — 실행일: 2026-06-04]   ← 날짜만 그날로 변경

오늘 자동배포(KST 05:23 cron)가 성공했어. 아래 순서로 종합 점검하고 섹션별로
PASS / WARN / FAIL 로 보고해줘. 문제가 있으면 수정안을 먼저 제시하고 내 승인을 받은 뒤
수정해. 빌드 검증과 커밋/푸시는 모든 점검·수정이 끝난 뒤, 내가 지시할 때만 1회 진행해.

■ 맥락 (매일 동일)
- 파이프라인: fetch-public-data.js → dedup-festivals-v2.js → build-topic-report.js(일일 후보 보고서)
  → [2026-06-07 [3b] 축제 묶음 자동발행: KST 목요일만 POST_TYPE=festival generate-blog-post / 옛 1:1은
  SHELVED 폐기] → validate-content.js --fix --quarantine → build → wrangler deploy. 데이터/보고서는 main에 되커밋됨.
  ※ 신규 블로그 글: **목요일 = 축제 묶음 1편(5건 미만 스킵) / 그 외 = 0건**이 정상. 묶음 발행 점검은 [E] 참조.
- 데이터 원천 1개: public/data/pick-info.json (benefits + festivals).
  /benefits = 전체 benefits 노출, /situations = 정규식 필터 뷰(원본 복제 아님).
- '신규' = 실행일(오늘)과 전일(어제)에 추가된 항목·글. addedAt 또는 파일/슬러그 날짜로 판별.
- 기존 중복 방지 장치(이미 통과했어야 정상):
  · 블로그 isDuplicate(generate-blog-post.js): posts/drafts/review 3폴더 스캔 +
    제목 norm 일치/포함/prefix + 키워드 Jaccard 0.5 + 동일 제도·행사명 공유 차단
  · 축제 dedup-festivals-v2.js: 같은 날짜 + 장소 + 제목 키워드 겹침 스마트 중복 제거
  · 지원금 findBenefitDuplicate(fetch-public-data.js): 자치구 가드 + bigram Jaccard 0.42
    + 문자열 포함 + 포함률 0.7 + prefix

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ [최우선 ★] 중복 콘텐츠 재발행 절대 금지 — 사이트 신뢰도 1순위 저격수
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
모든 축제/행사·지원금·블로그 글은 반드시 '서로 다른 고유 주제'여야 한다. 표현만 바꾼
재탕·동일 제도/행사의 중복 발행은 신뢰도를 가장 크게 떨어뜨리는 일이며 절대 통과시키지 않는다.
게이트를 통과했더라도 사람 눈으로 다시 잡아낸다. 다음을 전수 점검:

1) 블로그 글 중복 0
   - 신규 글의 제목·핵심 키워드·다루는 제도/행사명을 기존 글 전체(posts/drafts/review)와 대조.
   - 제목 표현만 다르고 같은 주제/제도면 FAIL (예: "청년 월세 20만원" vs "서울 청년월세 지원 정리").
   - 같은 축제/지원금을 날짜·연도·수식어만 바꿔 다시 쓴 글 0.

2) 지원금(benefit) 중복 0
   - 신규 benefit 제목을 기존 전체와 대조(자치구 별칭·stem 통일 반영: "용산"="용산구").
   - 같은 제도의 다른 표현 쌍 0. 의심쌍은 제목을 나란히 나열.

3) 축제/행사 중복 0
   - 같은 행사가 연도/표현만 바꿔 중복 등록되지 않았는지(같은 날짜+장소+행사명 키워드 겹침).

4) 교차 중복 점검 (benefit ↔ blog ↔ festival)
   - 같은 주제를 '동일 제목/본문 복제' 수준으로 양쪽에 올리지 않았는지.
   - (주의: 같은 지원금을 카드 + 해설 블로그로 '서로 다른 내용'으로 보강하는 건 정상.
      문제는 내용까지 사실상 복제된 경우만 FAIL.)

5) 발견 시 처리
   - 더 빈약한 쪽 삭제 + 풍부한 쪽 보존(필요 필드 이식).
   - 왜 게이트를 통과했는지 원인 분석 → dedup 임계/정규화/제도명 사전 보강안 제시.

6) ★ 회귀 감시 — 'Gemini 생성 후 최종 제목' 중복 (6/4 사고 재발 방지)
   ⏸ 자동발행 셸브(2026-06-05)로 현재 비활성 — 손글 발행 재개 또는 [SHELVED] 해제 시 재가동.
   - 배경: isDuplicate는 '크롤링 원본 제목(item.title)' 기준으로만 돈다. 소스 레코드가 서로
     다르면(예: 원본이 "[경기 화성] 노숙인시설 종사자 웰빙보조비" / "[서울 은평] 고독사 위험
     중장년…") 게이트를 통과하지만, Gemini가 둘 다 동일 결과물("[서울 용산] 자립준비청년
     생활보조수당")로 재작성하면 최종 제목이 충돌한다. → 2026-06-04 용산 자립준비청년글 2건
     재탕(6/3·6/4)이 이 경로로 빠져나갔고 6/4글을 삭제 처리함.
   - 매일 점검: 신규 블로그의 '최종 title'을 norm(괄호·태그·연도·지역 제거)하여 기존 전체와
     앞 10자 일치/포함/제도명 공유로 재대조. 특히 '자립준비청년 생활보조수당' 등 같은 제도가
     원본만 다른 채 다시 등장했는지 확인. 발견 시 빈약한 쪽 삭제.
   - 근본 수정: ✅ 적용 완료(2026-06-04, commit 3297059). generate-blog-post.js 파일 쓰기
     직전에 'Gemini 생성 후 최종 title' 기준 isDuplicate 재검사 패스가 들어가 있다
     (line ~872, "[중복 차단·생성후]" 로그). 매일 점검: 신규 글 최종 제목 중복이 0인지
     확인하고, 배포 로그에 "[중복 차단·생성후]"가 찍혔다면 정상 작동(차단 성공)으로 간주.
     혹시 이 재검사 블록이 사라졌거나 우회된 중복이 또 보이면 회귀로 보고.

■ [A] 검증 게이트 재현
- node scripts/validate-content.js (읽기 전용, --fix 없이).
- BLOCK / 격리 = 반드시 0. 1건이라도 있으면 FAIL로 즉시 보고.
- 자동수정(표 정규화 등) 내역 요약. WARN은 '신규 글'만 추려서 평가(과거 백로그 WARN은 건수만).
  (신규 블로그 글 기준: **KST 목요일 = 묶음 1편 정상 / 그 외 요일 = 0건 정상**(2026-06-07 [3b] 묶음 발행 활성).
   목요일 신규 묶음 글의 WARN은 [E]에서 평가. 데이터 검증은 매일 돔.)

■ [B] 신규 크롤링 데이터 정합성 (오늘·어제 추가분만)
- 비수도권(서울/인천/경기/전국 외) 유입 0 — region 값 + 제목 지역명 둘 다.
- 기업/홍보/정치/선거성 항목 유입 0.
- benefit 필수 필드 채움: faq / rejectionReasons / addedAt / simulation 존재. highlight 권장.
- 깨진 표 0 (festival·benefit 본문): 구분선에 설명문이 섞인 깨진 블록 없는지.
- 축제 제목 SEO 포맷 점검: 모든 festivals title이 `[서울|인천|경기|전국]`로 시작하는지,
  연도(20XX) 포함하는지, 클리셰("완벽 가이드/총정리/A-Z/한방에")가 없는지 전수 확인.
  · 문화행사 API(서울/경기/인천/KTO) 원본 제목은 태그·연도가 없어 들쑥날쑥했음 →
    seoFestivalTitle()로 정규화됨. 태그 없는/영문/연도 없는 신규 축제 제목이 보이면 회귀 보고.
- 지원금 제목 SEO 점검: 모든 benefits title이 `[지역]`(서울/인천/경기/전국 또는 시군구)로
  시작하는지, 클리셰("완벽 가이드/완벽 분석/총정리/A-Z/한방에")가 없는지 전수 확인.
  태그 없는/클리셰 포함 신규 지원금 제목이 보이면 보고.
- 카드 이미지 중복/generic 점검: 축제(festivals) image 필드를 전수 집계.
  · fallback-festival-1~5 또는 동일 unsplash URL이 여러 카드에 반복되면 회귀 — 보고.
  · 특히 경기·인천처럼 원본 이미지 미제공 출처의 신규 축제가 같은 이미지로 몰리지 않는지.
  · 정상 동작 시: 이미지 없는 축제는 pickThemedFestivalImage가 로컬 테마 풀
    (festival-* 이미지)에서 제목 주제에 맞는 '고유' 이미지를 배정함(무비용·외부호출 없음).
    image 값이 fallback-festival-N 로 떨어진 신규 축제가 있으면 풀 고갈/주제 미스이므로
    원인 보고(테마 풀 보강 또는 키워드 매핑 추가 제안).
- ★ 카테고리 편중 감시 (2026-06-05부터 누적 추적):
  · 배경: 복지로 중앙부처 API(NationalWelfarelistV001)는 srchKeyCode=003 고정 + 첫 페이지
    20건만 수집하는 구조라, 특정 주제(예: 장애인·보훈)에 편중될 수 있음. 2026-06-04 활성화
    검증 시 상위 20건 중 다수가 장애인·보훈 항목이었음.
  · ★ 분류 기준(2026-06-06 갱신): situations.ts가 **다중소속**(한 지원금이 여러 생애주기에 동시 소속)
    으로 전환됨 → 분포는 '단일 합32 버킷'이 아니라 **'페이지별 노출 건수'(합 32 초과 가능)**다.
    소속 판정은 benefitBelongsTo(제목 titleStem || 본문 compound). 이전의 'parenting 먼저 선점·단일버킷'
    분포(parenting22·housing0류)는 폐기 — 그 수치로 편중을 판단하지 마라.
  · 점검: 실행일 기준 누적된(2026-06-05 이후 추가분) benefit 항목을 5개 생애주기 카테고리
    (임신·출산·육아 / 청년 / 중장년·은퇴 / 주거·전월세 / 장애인·돌봄)별 **노출 건수**로 집계해 표로 보여줘라.
    housing 판정은 '미러 0'이 아니라 **라이브 getBenefitsForSituation(housing) 반환 건수**로 본다.
    (출처별 분할은 benefit에 _source가 미저장이라 현재 불가 — 필요 시 별도 백로그.)
  · 판정: 특정 한 카테고리(특히 장애인·돌봄)가 과도 편중되거나, 청년·주거 카테고리가
    며칠째 0건이면 WARN으로 보고하라. (다중소속이라 합이 32를 넘는 것은 정상 — 편중은 개별 카테고리
    노출 건수의 균형으로 판단.) 어느 출처에서 편중이 오는지 가능한 범위에서 명시하라.
  · 주의: 이는 '데이터 깨짐'이 아니라 '주제 다양성' 이슈다. 편중이 확인되면 원인(srchKeyCode
    고정 / 페이지 고정 / DAILY_LIMIT 5건이 앞쪽만 수집)을 짚고, 개선 방향(srchKeyCode 변경,
    페이지 순환, 카테고리 균형 수집 등)을 '제안'만 하라. 자동 수정하지 마라.
- ★ 데이터랩 작동 확인 (2026-06-05 활성화):
  · 배경: deploy.yml에 NAVER_CLIENT_ID/SECRET를 추가해 네이버 DataLab 기반 인기 키워드
    블로그 후보 선정을 켰음(이전엔 키 부재로 계절 키워드 폴백으로 동작).
  · 점검: 당일 배포 로그에서 DataLab 호출 관련 메시지를 확인하라. '키 없음'·'계절 폴백' 류
    메시지가 더 이상 출력되지 않으면 정상 작동(PASS)으로 본다. 폴백 메시지가 여전히 보이면
    GitHub Secrets 등록 또는 env 주입 문제이므로 WARN으로 보고하라.
  · 호출 실패 시(네트워크·4xx/5xx·한도 초과) 코드가 계절 폴백으로 안전하게 회귀하므로
    사이트가 깨지진 않으나, '인기 기반 선정'이라는 의도가 작동하지 않는 상태이므로 보고 대상이다.
- ★ 축제 발행 게이트 확인 (2026-06-04 추가):
  · ⏸ 자동발행 셸브(2026-06-05)로 현재 비활성 — 손글 발행 재개 또는 [SHELVED] 해제 시 재가동.
  · 배경: 축제 블로그에 '미발행 후보 3건 미만이면 발행 건너뜀' 게이트를 추가했음
    (지원금과 동일하게 PUBLISH_THRESHOLD=3 기준). 데이터가 부족할 때 억지 발행을 막는 장치.
  · 점검: 축제 미발행 후보가 3건 미만인 날, 배포 로그에 '[축제] … 발행 건너뜀'이 찍혔는지
    확인하라. 찍혔다면 게이트 정상 작동(PASS). 반대로 후보가 3건 미만인데도 축제 블로그가
    새로 발행됐다면 게이트 우회·회귀이므로 FAIL로 보고하라.
- ★ 블로그 본문 표 무결성 (신규 블로그 글 대상, 2026-06-05 추가):
  · ⏸ 자동발행 셸브(2026-06-05)로 현재 비활성 — 손글 발행 재개 또는 [SHELVED] 해제 시 재가동.
  · 배경: 6/4 fixTableCells 게이트는 '닫히지 않은 셀'만 봤고 헤더 누락·표 중간 빈줄은 사각지대였다.
    6/5에 dropTableInnerBlanks·ensureTableHeader를 validate-content.js에 추가해 자동 교정 중(commit 1403412).
    게이트가 막는 것과 사람이 눈으로 확인하는 것은 별개 — 6/5 꽃축제글 사고는 게이트는 있었으나 점검 항목이 없어 통과했다.
  · 점검: src/content/posts 의 신규(.md) 글을 스캔해 아래 3패턴이 각각 0건인지 확인하라.
    - `:---` 구분선이 헤더 없이 첫 줄로 시작하는 표(헤더 누락) 0건.
    - 표 데이터 행 사이 빈 줄 0건.
    - 본문에 `|` 또는 `:---`가 글자 그대로(렌더 안 되고 평문으로) 노출된 글 0건.
  · 판정: 하나라도 발견되면 WARN으로 보고하되, 원인이 generate-blog-post.js의 표행 절단(table-aware 후처리)인지
    Gemini 원출력인지 구분해 명시하라.
- ★ ensureTableHeader WARN 로그 확인 (2026-06-05 추가):
  · ⏸ 자동발행 셸브(2026-06-05)로 현재 비활성 — 손글 발행 재개 또는 [SHELVED] 해제 시 재가동.
  · 배경: ensureTableHeader는 헤더가 없으면 일반 라벨로 헤더를 합성해 끼워넣는데, 합성 라벨이 실제 표 내용과
    어긋날 수 있다. 게이트가 '교정함'으로 끝내면 안 되고 사람이 한 번 본다.
  · 점검: 당일 검증 로그에 '헤더 없는 표 감지 → … 헤더 합성' WARN이 찍힌 글이 있으면, 그 글의 합성된 표 헤더
    라벨이 실제 셀 내용과 의미상 맞는지 육안 확인하라. 어긋나면 라벨 수정을 '제안'으로 보고하라(자동 수정 금지).

■ [C] ⚠️ 형식·엣지 변종 탐지 (매일 새 주제에서 튀어나옴 — 적극적으로 찾아낼 것)
신규 + 전체를 스캔해 우리 로직의 가정을 깨는 패턴을 능동적으로 탐지:
- deadline 형식 변종: 단일 날짜(2026.06.30) / 범위(~) / 한글("매년 5월","상시","별도 공지")
  / 요일·시간 포함 / 공백·구분자 변형. 각 변종이 정렬(마감임박순 버킷)·D-Day 배지·
  상시/모집중/마감 판정에서 의도대로 동작하는지. (※ 6/3 버그가 바로 단일 마감일 오판이었음.)
- region 값이 정확히 4종(서울/인천/경기/전국)인지, 시군구 단독 표기 등 이상값 없는지.
- 금액 추출이 실패하거나 엉뚱한 수치(소득 기준·재산 문턱·나이·면적·연도)를 금액으로
  오추출하지 않는지. 신규 항목 추출 결과를 실제 혜택과 대조해 의심 건 나열.
- title/target에 HTML 태그·깨진 문자·비정상 길이·프롬프트 에코("~해줘","예:") 혼입 0.
- 어떤 situations 정규식에도 안 걸리는 '고아 지원금'이 전일 대비 비정상 급증했는지.

■ [D] /benefits 화면 로직
- 정렬 3종 검증: 마감임박순=마감일 있는 진행 항목이 D-Day 가까운 순으로 최상단, 상시는 그 뒤,
  종료(마감)는 항상 맨 뒤 / 최신등록순=addedAt 최신순 / 금액높은순=추출 금액 내림차순.
  각 상위 5건 표로.
- 배지 일관성: 상시/모집중/마감임박/NEW/D-Day 가 마감일 기준으로 모순 0
  (상시인데 마감임박, 종료인데 NEW 등 금지).

■ [E] 축제 묶음 자동발행 점검 (2026-06-07 [3b] 활성)
※ 이력: 2026-06-05 1:1 자동발행 셸브로 ⏸였으나, 2026-06-07 [3b]로 '축제 묶음 자동발행'(KST 목요일만)이
  활성화됨. 옛 1:1(SHELVED)은 폐기. 아래는 묶음 발행 기준 점검(신규 묶음 글 대상).
- 글 품질 공통: 분량(공백 제외 1,500자 이상 지향) / H2 3개 이상 / 비교표 유무 / 제목 클리셰
  ("총정리/완벽 가이드/A-Z/한방에") 0 / 프롬프트 에코 0. 제목 클리셰는 전수로도 0(재등장 시 생성기 회귀 보고).
- ★ 발행 요일 정합: **목요일** 배포 = 새 묶음 .md 1편 생성(또는 '5건 미만 → 발행 스킵' 로그). **비목요일**
  배포 = 새 festival .md 0이 정상. 목요일 아닌데 새 묶음 festival 글이 생겼으면 가드 우회·회귀로 WARN.
- ★ 묶음↔본문 정합(누수 감시): 새 글 frontmatter `sourceIds` 개수 = 본문이 실제 개별 서술한 축제 수.
  불일치(예 sourceIds 8 vs 본문 5)면 누수 — sourceIds엔 있으나 본문 누락분이 다음 주 직전제외로 영영
  안 쓰임. WARN 보고(3a-4에서 푼 결함의 사후 감시).
- ★ 직전 제외 작동: 이번 주 글 sourceIds ∩ 직전 주 festival 글 sourceIds = **0**(연속주 중복 차단).
  단 첫 발행(6/11)은 직전 글에 sourceIds가 없어 제외 0이 정상.
- ★ sourceIds 유효성: sourceIds 전부가 그날 pick-info festivals의 실재 id인지. 존재하지 않는 id가
  섞이면 WARN(셀렉터/주입 회귀 의심).
- ★ 시기 제목: `title`이 `"M월 W주 주말 수도권 가족축제"` 형식·주차(W)가 발행일 기준 정확한지
  (W = 그 달 ceil(일/7)).
- ★ 상설 미혼입: 묶음 항목에 🔄(연중상설 = span≥90일 ‖ 상시/수시/연중/매월/매년/정기/모집중) **0건**
  (셀렉터 상설 제외 사후 확인). 섞였으면 WARN.
- ★ 쿠팡 0: 새 글에 `쿠팡`·`coupang`·`파트너스` 흔적 0(3a-6 제거 사후 감시). 보이면 생성기 회귀로 WARN.
- 비교표 완전성: 비교표 데이터 행 수 = 묶음 축제 수. 미달이면(flash 부분준수 — 코드 비보장) WARN +
  백로그 누적(비차단, 코드 조립은 추후 빈도 보고 결정).
- dedup 경로: 묶음 글은 bundleMode(rule1 완전일치만, rule2~5 면제) 적용이라 같은 주 재발행 외엔
  정상 통과. 배포 로그에 '[중복 차단·생성후]'가 같은 주 아닌데 찍혀 묶음이 막혔으면 회귀로 보고.

■ [F] 라우팅·정합성
- 과거 삭제한 블로그 슬러그 6종 재생성 0 (⏸ 자동발행 셸브로 현재 신규 블로그 0건 — 슬러그 재생성 위험은 손글 발행 시에만. _redirects 이하는 계속 유효) / public/_redirects 규칙 유지 /
  benefits·festivals 카운트 및 situations 허브별 매칭 건수 갱신 정상 / sitemap.xml 정상.
- URL 안정성(churn 추적): 신규 benefit/festival id 가 결정적 형식인지 확인
  (b-, fest-, fest-seoul-, fest-gg-, fest-ic-, gemini-* + 해시). id 안에 13자리
  타임스탬프(Date.now)나 난수 꼬리표가 보이면 회귀 — 같은 제도가 매일 새 URL을 만들어
  GSC 404가 누적되므로 즉시 보고. (참고: GSC '페이지' 보고서의 404/리디렉션 추세가
  주는 줄어들어야 정상.)

■ [G] '어제 작업이 오늘 라이브에 반영됐는지' 확인
- origin/main HEAD == 실제 배포 커밋(빌드 기준)과 동기화.
- 라이브 tip-pick.com/benefits 에 ① 2열 카드 그리드 ② 정렬 드롭다운 3종 ③ 💰 금액 강조
  ④ 주거·임대 탭/진행상태 드롭다운 부재 반영(캐시 우회 ?v=커밋해시). 마감임박 D-Day 항목이
  실제 최상단인지 눈으로 확인.

■ [H] 일일 후보 보고서 (docs/daily-topic-report.md, 2026-06-05 추가)
· 배경: 블로그 자동발행 셸브 후, 크론 dedup 뒤 build-topic-report.js가 축제·지원금 후보를
  핫스코어로 랭킹한 '읽기전용' 보고서를 매일 생성(글 미발행). 운영자 손글 글감 선별용.
· 생성 확인: docs/daily-topic-report.md가 오늘 자 데이터 커밋에 포함돼 갱신됐는지 —
  파일 존재 + 머리말 `> YYYY-MM-DD 생성 · 읽기전용(글 발행 안 함) · 손글 .md 글감 선별용`의
  날짜가 실행일과 일치.
· 실패 가드: 스크립트는 실패해도 죽지 않고(배포 본류는 `|| true`) 그 파일에 머리말 다음
  `**생성 실패: <이유>**` 한 줄을 굵게 남긴다. 그 문구가 보이면 원인(pick-info 파싱 실패 등) 보고.
· 상단 판정 일치: 보고서 상단이 `✅ **쓸 거리 있음** — 축제 N건 · 지원금 M건 (한쪽이라도 ≥3)`
  또는 `⏸️ **오늘은 패스 권장** — 축제 N건 · 지원금 M건 (양쪽 <3, 케이던스 원칙상 강제 아님)`
  중 무엇인지, 그 N·M이 실제 pick-info(만료 제외) 건수와 맞는지 교차검증.
· DataLab 호출: 보고서 머리 `핫키워드 출처 — 축제: \`datalab\` (…) / 지원금: \`datalab\` (…)`로
  festival·benefit 각 1회(총 2회) source가 `datalab`인지 확인. `seasonal`/`seasonal(api-error)`/
  `seasonal(exception)`로 표시되면 키 부재·오류 폴백이므로 보고.
· 마감임박·이벤트성 콜아웃: `🔥 마감임박`(deadline 파싱돼 0~7일)·`🎟️ 이벤트성·단발`(상시·수시·
  연중·매월·매년·정기류가 아니며 종료일 실재)에 뜬 항목의 D-day·기간이 실제 pick-info deadline과
  맞는지 표본 확인.
· 생애주기 버킷 오분류(백로그 12): `👶 임신·출산·육아` 버킷에 노숙인 보조비류 등 무관 항목이
  섞여 있으면 건수만 기록(수정 금지 — situations.ts SSOT 정밀화는 별도 백로그 12).
· ★ 추천 글감 섹션·딱지(2026-06-06 §4-16 추가): ⓐ 보고서 맨 위 `## ⭐ 오늘의 추천 글감`이 존재하고,
  `📝 지원금 블로그 후보`가 **마감 가까운 순**(점수순 아님·D-day 오름차순)인지 / `🎪 축제 묶음 후보`가
  ✅ 5건 이상이면 '이 묶음으로…' 제안, 5건 미만이면 '기준 미달' 표시인지 확인.
  ⓑ 축제 표 `시의성` 컬럼(✅/🌱/⏳)·지원금 표 `용도` 컬럼(📝/📚)이 존재하고, 딱지가 규칙에 부합하는지
  스팟 1~2건 육안: 시의성 = 종료 7일내 ⏳ / 진행중·21일내 시작 ✅ / 21일 초과 미래 🌱(만료는 표에서 제외).
  용도 = 실재 미래 마감 단발 📝 / 상시·정기·미파싱 📚. 어긋나면 항목·사유 보고(자동 수정 금지).

■ 보고 규칙
- [최우선★]·[A]~[H]를 PASS/WARN/FAIL 로 요약. FAIL/의심 건은 항목·원인·수정안 제시.
- 수정은 승인 후. 빌드 검증·커밋/푸시는 지시 시 1회.
```

---

## 변경 이력
- 2026-06-03 최초 작성. 6/3 작업(중복정리·표 게이트·/benefits 카드 개편+정렬 3종·정렬 버그 수정) 반영.
  '중복 콘텐츠 재발행 절대 금지'를 최우선 섹션으로 명시.
- 2026-06-04 점검 결과 반영. 용산 자립준비청년 생활보조수당 블로그 2건(6/3·6/4) 재탕 발견 →
  빈약한 6/4글 삭제. [최우선★]에 6) '생성 후 최종 제목 중복' 회귀 감시 항목 추가
  (isDuplicate가 원본 제목만 비교해 Gemini 재작성 충돌을 못 잡은 사고).
- 2026-06-04 근본 수정 적용(commit 3297059). generate-blog-post.js에 '생성 후 최종 title'
  isDuplicate 재검사 패스 추가 → 6)의 근본 수정 항목을 '적용 완료'로 갱신.
- 2026-06-05 [B]에 '카테고리 편중 감시' 항목 추가. 복지로 중앙부처(V001) 활성화(2026-06-04)
  후 특정 주제 편중 가능성에 대비해, 6/5부터 누적 카테고리·출처별 분포를 추적.
- 2026-06-05 [B]에 '데이터랩 작동 확인'·'축제 발행 게이트 확인' 점검 항목 추가
  (각각 DataLab 활성화·축제 3건 게이트 적용에 대응).
- 2026-06-05 자동발행 셸브 반영 — 블로그 발행 관련 점검 7곳([최우선★]6·[A]신규블로그WARN·축제
  발행게이트·블로그표무결성·ensureTableHeader·[E]·[F]슬러그재생성)을 '⏸ 비활성(이력 보존)'로 표시,
  [H] 일일 후보 보고서 점검 신설. 크롤 관련 점검은 유지.
- 2026-06-07 [3b] 축제 묶음 자동발행 활성화 반영 — [E]를 '축제 묶음 자동발행 점검'으로 갱신(⏸→활성):
  발행 요일 정합·묶음↔본문 정합(누수)·직전제외·sourceIds 유효성·시기제목·상설 미혼입(🔄)·쿠팡 0·
  비교표 완전성·dedup(bundleMode) 9항목 추가. [A] WARN 기준을 '목요일 1편/그 외 0'으로, 맥락 파이프라인을
  묶음 발행으로 갱신. (나머지 ⏸ 항목은 묶음과 무관/저관여라 이번 미변경 — 필요 시 재방문.)
