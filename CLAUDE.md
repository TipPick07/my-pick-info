@AGENTS.md

# 작업 시작 규칙 (항상 적용)

- 이 저장소에서 새 작업을 시작하기 전에 **먼저 `docs/HANDOVER.md`를 읽어** 프로젝트 맥락(정체성·글쓰기 규칙·현재 현황·백로그)을 복원한 뒤 진행한다.
- 글을 작성할 때는 HANDOVER §5 규칙을 반드시 따른다:
  1. **같은 카테고리의 기존 글 형식을 그대로** 따른다(새 형식을 만들지 않는다).
  2. 체류시간 확보를 위해 **글→글 내부 루프 링크**(마무리에 형제 글·계산기, 본문 맥락 링크)를 넣는다.
- 수치·사실은 공식 자료로 확인하고, 확실하지 않으면 단정하지 않는다(계산기·세금 수치는 검산 필수).
- **커밋·푸시는 운영자가 명시적으로 지시할 때만** 한다(자동 금지).
- 작업이 끝나면 `docs/HANDOVER.md` §9 작업 로그에 `### 날짜 — 한 줄 요약`을 추가한다.
